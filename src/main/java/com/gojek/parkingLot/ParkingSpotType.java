package com.gojek.parkingLot;

// currently all slot type are same but in future can have
// differnet slot type like for wallet and premium parking etc
public enum ParkingSpotType {
    CAR, ELECTRIC_CAR, PREMIUM_SPOT, WALLET_SPOT
    }
